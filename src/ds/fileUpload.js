import { FileUpload } from "@dsyd/com";

<FileUpload
    value={value}
    maxLength={5}
    showUploadList={{ showDownloadIcon: true }}
    onChange={onChange}
    templateUrl={'111'}
    disabled={disabled}
/>